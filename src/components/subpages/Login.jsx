import LoginForm from "../features/LoginForm"
import Wrapper from "../shared/Wrapper"

const Login = () => {
    return (
        <Wrapper>
            <LoginForm/>
        </Wrapper>
    )
}

export default Login